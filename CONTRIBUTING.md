# Contributing

1. Branches verwenden (`feat/...`, `fix/...`, `docs/...`)
2. Commits folgen den Conventional Commits (z.B. `feat(orders): ...`)
3. Lokale Tests durchführen (Frontend & Backend & DB)
4. Pull Requests erstellen und reviewen lassen
5. Auf GitHub Actions (CI) achten
